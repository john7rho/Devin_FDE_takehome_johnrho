# Dependencies (installed fresh inside the image)
node_modules

# Next.js build output (rebuilt inside the image)
.next
.next.broken
out
build

# Env / secrets
.env*.local

# Git / editor / OS
.git
.gitignore
.vscode
.idea
*.swp
.DS_Store

# Docker
Dockerfile
.dockerignore

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
